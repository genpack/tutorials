To download and copy a file from an s3 bucket:

aws s3 cp <file_url> <target_location> —profile <profile_name>

Example:

aws s3 cp s3://sticky.dummy.elula.ai/config.infer.yml . --profile write@sticky


How to do it in python:


>>> import boto3
>>> from urllib.parse import urlparse
>>> aws_s3 = boto3.client('s3')
>>> o = urlparse(file_url)
>>> aws_s3.download_file(o.netloc, o.path.lstrip('/'), filename)

This copies the file into the current working directory
To get current working directory:
>>> import os
>>> os.getcwd()

